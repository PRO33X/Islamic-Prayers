pluginManagement {
	repositories {
		gradlePluginPortal()
		google()
		mavenCentral()
	}
}

dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
	repositories {
		google()
		mavenCentral()
		maven { url = 'https://jitpack.io' }
	}
}

rootProject.name = "Prayers"

include(":app")
include(":locationutils")
include(":Islamic")

project(":locationutils").projectDir = file("libraries/locationutils")
project(":Islamic").projectDir = file("libraries/Islamic")
